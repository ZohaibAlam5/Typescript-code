let current_users: string[] = ["ali", "Zohaib", "imran", "haseeb", "riaz"]

let new_users: string[] = ["hanzala", "reyan", "ali", "imran", "fasih"]

for(let newuser of new_users)

if(new_users == current_users){console.log(`${newuser} you need to emter a new username`)}
else(console.log("username is availale"))