function cars(Manufacturer:string, Modelname: string, Color:string, Options: string){
    let car = { 
        Manufacturer,
        Modelname,
        Color,
        Options

}
return car
}

console.log(cars("Rolls Royce", "Phantom", "White", "Cruise control"))